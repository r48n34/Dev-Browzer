{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Permissions for the bundled Dev Browzer workbench shell only.",
  "webviews": ["main"],
  "permissions": ["core:default", "store:default"]
}
